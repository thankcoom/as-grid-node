apiVersion: zeabur.com/v1
kind: Template
metadata:
  name: Louis AS Grid Node
spec:
  description: 自動化網格交易機器人節點 - 24/7 智能交易
  icon: https://louisasgrid-web.zeabur.app/logo.png
  coverImage: https://louisasgrid-web.zeabur.app/logo.png
  tags:
    - Trading
    - Bot
    - Crypto
  variables:
    - key: BITGET_UID
      type: STRING
      name: Bitget UID
      description: 您的 Bitget Exchange UID（10位數字，可從 Bitget 個人中心查看）
    - key: EXCHANGE_API_KEY
      type: STRING
      name: API Key
      description: Bitget API Key（需勾選合約交易權限）
    - key: EXCHANGE_SECRET
      type: STRING
      name: API Secret
      description: Bitget API Secret
    - key: EXCHANGE_PASSPHRASE
      type: STRING
      name: Passphrase
      description: Bitget API Passphrase（創建 API 時設定的密碼）
    - key: NODE_DOMAIN
      type: DOMAIN
      name: 節點網域
      description: 為您的交易節點選擇一個網域
  readme: |
    # Louis AS Grid Node
    
    此節點將連接到官方伺服器，執行網格交易。您的 API 憑證安全地存儲在您自己的節點中。
    
    ## 🔐 部署前準備
    
    1. **前往 [Bitget](https://www.bitget.com)** 登入您的帳號
    2. **個人中心 → API 管理 → 創建 API**
    3. 勾選 **合約交易** 權限（必要）
    4. 記下您的 **API Key**、**API Secret**、**Passphrase**
    5. 在個人中心查看您的 **UID**（10 位數字）
    
    ## 🌐 官網
    
    **[前往 Louis AS Grid 官網 →](https://louisasgrid.zeabur.app)**
    
    ## 部署後操作
    1. 等待部署完成（約 1-2 分鐘）
    2. 複製生成的網域（如 `xxx.zeabur.app`）
    3. 返回 [官網設定頁面](https://louisasgrid.zeabur.app/settings)，貼上網域並儲存
    4. 開始交易！
    
    ## 安全說明
    - ✅ API 憑證僅存儲在您的節點中，不經過官方伺服器
    - ✅ 節點與官方透過安全的 JWT Token 通訊
    - ⚠️ 請勿將 API Key 分享給任何人
    
    ## 聯繫我們
    - 官網：https://louisasgrid.zeabur.app
    - 支援信箱：support@louislab.com
    
  localization:
    en:
      description: Automated grid trading bot node - 24/7 smart trading
      variables:
        - key: BITGET_UID
          name: Bitget UID
          description: Your Bitget Exchange UID (10-digit number from Bitget profile)
        - key: EXCHANGE_API_KEY
          name: API Key
          description: Bitget API Key (must have Futures trading permission)
        - key: EXCHANGE_SECRET
          name: API Secret
          description: Bitget API Secret
        - key: EXCHANGE_PASSPHRASE
          name: Passphrase
          description: Bitget API Passphrase (set when creating the API)
        - key: NODE_DOMAIN
          name: Node Domain
          description: Choose a domain for your trading node
      readme: |
        # Louis AS Grid Node
        
        This node connects to the official server and executes grid trading. Your API credentials are securely stored on your own node.
        
        ## 🔐 Before Deployment
        
        1. **Go to [Bitget](https://www.bitget.com)** and log in
        2. **Profile → API Management → Create API**
        3. Enable **Futures Trading** permission (required)
        4. Note your **API Key**, **API Secret**, **Passphrase**
        5. Find your **UID** in profile (10-digit number)
        
        ## After Deployment
        1. Wait for deployment (~1-2 minutes)
        2. Copy the generated domain (e.g., `xxx.zeabur.app`)
        3. Return to [Official Settings](https://louisasgrid.zeabur.app/settings), paste and save
        4. Start trading!
        
        ## Security
        - ✅ API credentials are stored ONLY on your node
        - ✅ Secure JWT communication with official server
        - ⚠️ Never share your API Key with anyone
        
        ## Contact
        - Website: https://louisasgrid.zeabur.app
        - Email: support@louislab.com
        
  services:
    - name: grid-node
      icon: https://louisasgrid-web.zeabur.app/logo.png
      template: PREBUILT
      domainKey: NODE_DOMAIN
      spec:
        source:
          image: ghcr.io/thankcoom/as-grid-node:latest
        ports:
          - id: web
            port: 8000
            type: HTTP
        env:
          AUTH_SERVER_URL:
            default: https://louisasgrid.zeabur.app
            readonly: true
          BITGET_UID:
            default: ${BITGET_UID}
          EXCHANGE_API_KEY:
            default: ${EXCHANGE_API_KEY}
          EXCHANGE_SECRET:
            default: ${EXCHANGE_SECRET}
          EXCHANGE_PASSPHRASE:
            default: ${EXCHANGE_PASSPHRASE}
          NODE_SECRET:
            default: ${PASSWORD}
            expose: true
        instructions:
          - type: TEXT
            title: Node URL
            content: https://${NODE_DOMAIN}
          - type: TEXT
            title: Official Website
            content: https://louisasgrid.zeabur.app
          - type: TEXT
            title: Bitget UID
            content: ${BITGET_UID}
          - type: PASSWORD
            title: Node Secret
            content: ${NODE_SECRET}
            category: Credentials

